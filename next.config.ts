// next.config.ts

import { NextConfig } from 'next'

const nextConfig: NextConfig = {
  images: {
    domains: ['ecommerce.routemisr.com','images.unsplash.com'],  // this code to let next js accept images from this domaine
  },
}

export default nextConfig
